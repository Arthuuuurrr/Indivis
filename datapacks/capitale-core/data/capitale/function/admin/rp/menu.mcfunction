# Fallback RC9ae : le menu RP complet n'est pas inclus dans ce core réduit.
tellraw @s {"text":"[Admin RP] Menu RP détaillé absent de ce build ; retour au menu admin central.","color":"gray"}
function capitale:admin/menu_self
