# CapSkills BETA 0.10.18g — Auto Combat Root

- `doctrine_root` reste `root: true`, coût 0.
- Déblocage forcé à chaque connexion via `puffish_skills skills unlock`.
- Marqueur `capskills.skill.doctrine_root` ajouté/retiré avec le reward du root.
- Vérification toutes les 5 secondes : si un reset verrouille la racine, elle est automatiquement réactivée.
- `voie_metiers` reste une racine indépendante et n’est pas auto-débloquée.
- Aucune coordonnée ni connexion modifiée.
