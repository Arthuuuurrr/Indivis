name: Package capitale-core

on:
  push:
    branches:
      - feature/indivis-corruption-biome
    paths:
      - '.github/workflows/package-capitale-core.yml'
      - 'datapacks/capitale-core/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4

      - name: Build complete datapack ZIP
        run: |
          cd datapacks/capitale-core
          zip -r ../../capitale_core_1.5.6-RC9AR_5_NEW_IMPERIAL_CITIES_FULL.zip .

      - name: Upload complete core
        uses: actions/upload-artifact@v4
        with:
          name: capitale_core_1.5.6-RC9AR_5_NEW_IMPERIAL_CITIES_FULL
          path: capitale_core_1.5.6-RC9AR_5_NEW_IMPERIAL_CITIES_FULL.zip
          if-no-files-found: error
          retention-days: 7
